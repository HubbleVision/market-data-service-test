# V1 Legacy Tests

This directory contains tests for V1-only endpoints that have no V2 replacement.

**When V1 is fully deprecated, delete this entire directory:**
```bash
rm -rf tests/v1/
```

No framework changes needed — framework layer is version-agnostic.
